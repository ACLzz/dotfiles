# Sway variables, not env

# Mod
set $mod Mod4

# Home row direction keys
set $left h
set $down j
set $up k
set $right l

# Terminal
set $term alacritty msg create-window

# Application Launcher
set $menu tofi-run | xargs swaymsg exec --
